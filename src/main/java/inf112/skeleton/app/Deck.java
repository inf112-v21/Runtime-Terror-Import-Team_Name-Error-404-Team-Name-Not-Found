package inf112.skeleton.app;

import java.util.ArrayList;
import java.util.List;

/*public class Deck<deck> {
    public List<getCards> deck{
        deck = new ArrayList<>(numberOfCards);
        for (int i = 0; i<18; i++){
            if (i < 6){
                deck.add(getCards.MOVE_BACKWARDS);
                deck.add(getCards.MOVE_FORWARDS_THREE);
                deck.add(getCards.TURN_AROUND);
            }
            if (i < 12){
                deck.add(getCards.MOVE_FORWARDS_TWO);
            }
            deck.add(getCards.MOVE_FORWARDS_ONE);
            deck.add(getCards.ROTATE_RIGHT);
            deck.add(getCards.ROTATE_RIGHT);
        }
        return deck;
    }
}*/
